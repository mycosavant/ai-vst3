name: Build VST - All Platforms

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: read
  pull-requests: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup MSVC
        uses: microsoft/setup-msbuild@v1

      - name: Configure CMake
        working-directory: vst
        run: |
          cmake -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

      - name: Build
        working-directory: vst
        run: cmake --build build --config Release

      - name: Upload Windows VST
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-Windows-VST3
          path: vst/build/ObsidianNeuralVST_artefacts/Release/VST3/

  build-macos-universal:
    name: Build macOS Universal (10.15+)
    runs-on: macos-12

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          brew install cmake ninja

      - name: Configure CMake
        working-directory: vst
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="10.15"

      - name: Build
        working-directory: vst
        run: cmake --build build --config Release

      - name: Fix macOS permissions
        run: chmod +x vst/build/ObsidianNeuralVST_artefacts/Release/VST3/OBSIDIAN-Neural.vst3/Contents/MacOS/*

      - name: Verify build
        run: |
          echo "=== Universal Build Info ==="
          echo "Architecture:"
          lipo -info vst/build/ObsidianNeuralVST_artefacts/Release/VST3/OBSIDIAN-Neural.vst3/Contents/MacOS/OBSIDIAN-Neural
          echo ""
          echo "Deployment target:"
          otool -l vst/build/ObsidianNeuralVST_artefacts/Release/VST3/OBSIDIAN-Neural.vst3/Contents/MacOS/OBSIDIAN-Neural | grep -A 5 LC_BUILD_VERSION

      - name: Upload macOS VST
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-macOS-Universal-VST3
          path: vst/build/ObsidianNeuralVST_artefacts/Release/VST3/

  build-macos-test:
    name: Test Build - macOS ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    if: github.event_name == 'pull_request' # Uniquement sur les PRs

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Ventura"
            runner: macos-13
            deployment_target: "13.0"
          - name: "Sonoma"
            runner: macos-14
            deployment_target: "14.0"
          - name: "Sequoia"
            runner: macos-15
            deployment_target: "15.0"

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: brew install cmake ninja

      - name: Configure CMake
        working-directory: vst
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="${{ matrix.deployment_target }}"

      - name: Build
        working-directory: vst
        run: cmake --build build --config Release

      - name: Verify deployment target
        run: |
          echo "=== Build for ${{ matrix.name }} ==="
          otool -l vst/build/ObsidianNeuralVST_artefacts/Release/VST3/OBSIDIAN-Neural.vst3/Contents/MacOS/OBSIDIAN-Neural | grep -A 5 LC_BUILD_VERSION

  test-macos-universal:
    name: Test Universal Build on macOS ${{ matrix.name }}
    needs: build-macos-universal
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-13
            name: "Ventura"
          - runner: macos-14
            name: "Sonoma"
          - runner: macos-15
            name: "Sequoia"

    steps:
      - name: Download Universal VST
        uses: actions/download-artifact@v4
        with:
          name: ObsidianNeural-macOS-Universal-VST3
          path: ./vst

      - name: Test VST loading
        run: |
          echo "=== Testing Universal Build on macOS ${{ matrix.name }} ==="
          VST_PATH="./vst/OBSIDIAN-Neural.vst3"

          if [ -d "$VST_PATH" ]; then
            echo "✓ VST found"
            
            echo ""
            echo "Architecture:"
            lipo -info "$VST_PATH/Contents/MacOS/OBSIDIAN-Neural"
            
            echo ""
            echo "Deployment target:"
            otool -l "$VST_PATH/Contents/MacOS/OBSIDIAN-Neural" | grep -A 5 LC_BUILD_VERSION
            
            echo ""
            echo "Dependencies (first 15):"
            otool -L "$VST_PATH/Contents/MacOS/OBSIDIAN-Neural" | head -15
            
            echo ""
            echo "✅ Universal build successfully loaded on macOS ${{ matrix.name }}"
          else
            echo "❌ VST not found"
            exit 1
          fi

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build \
            libasound2-dev libx11-dev libxext-dev libxrandr-dev libxcomposite-dev \
            libxinerama-dev libxcursor-dev libfreetype6-dev libfontconfig1-dev \
            libgtk-3-dev libwebkit2gtk-4.1-dev libcurl4-openssl-dev

      - name: Configure CMake
        working-directory: vst
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        working-directory: vst
        run: cmake --build build --config Release

      - name: Upload Linux VST
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-Linux-VST3
          path: vst/build/ObsidianNeuralVST_artefacts/Release/VST3/

  build-python-executables:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: linux
          - os: macos-12
            name: macos-intel
          - os: macos-latest
            name: macos-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          pip install pillow pyinstaller psutil pystray GPUtil

      - name: Build executables
        shell: bash
        run: |
          pyinstaller --onefile --hidden-import=psutil --hidden-import=pystray --hidden-import=GPUtil --name obsidian-server-${{ matrix.name }} server_interface.py
          pyinstaller --onefile --hidden-import=psutil --hidden-import=pystray --hidden-import=GPUtil --name obsidian-installer-${{ matrix.name }} installer.py

      - name: Upload Python executables
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-Python-${{ matrix.name }}
          path: dist/

  build-macos-packages:
    needs: [build-python-executables]
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Download macOS Python executables
        uses: actions/download-artifact@v4
        with:
          name: ObsidianNeural-Python-macos-intel
          path: ./python-tools-intel/

      - name: Download macOS ARM Python executables
        uses: actions/download-artifact@v4
        with:
          name: ObsidianNeural-Python-macos-arm64
          path: ./python-tools-arm64/

      - name: Create Intel DMG
        run: |
          mkdir -p dmg-staging-intel
          cp python-tools-intel/obsidian-installer-macos-intel dmg-staging-intel/OBSIDIAN-Neural-Installer
          chmod +x dmg-staging-intel/OBSIDIAN-Neural-Installer

          create-dmg \
            --volname "OBSIDIAN-Neural-Installer-Intel" \
            --window-pos 200 120 \
            --window-size 500 300 \
            --icon-size 100 \
            --icon "OBSIDIAN-Neural-Installer" 250 150 \
            --hide-extension "OBSIDIAN-Neural-Installer" \
            "OBSIDIAN-Neural-Installer-macOS-Intel.dmg" \
            "dmg-staging-intel/"

      - name: Create ARM DMG
        run: |
          mkdir -p dmg-staging-arm64
          cp python-tools-arm64/obsidian-installer-macos-arm64 dmg-staging-arm64/OBSIDIAN-Neural-Installer
          chmod +x dmg-staging-arm64/OBSIDIAN-Neural-Installer

          create-dmg \
            --volname "OBSIDIAN-Neural-Installer-ARM64" \
            --window-pos 200 120 \
            --window-size 500 300 \
            --icon-size 100 \
            --icon "OBSIDIAN-Neural-Installer" 250 150 \
            --hide-extension "OBSIDIAN-Neural-Installer" \
            "OBSIDIAN-Neural-Installer-macOS-ARM64.dmg" \
            "dmg-staging-arm64/"

      - name: Upload DMGs
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-macOS-DMGs
          path: |
            OBSIDIAN-Neural-Installer-macOS-Intel.dmg
            OBSIDIAN-Neural-Installer-macOS-ARM64.dmg

      - name: Create PKGs
        run: |
          # Intel PKG
          mkdir -p pkg-staging-intel/payload/usr/local/bin
          mkdir -p pkg-staging-intel/scripts

          cp python-tools-intel/obsidian-installer-macos-intel pkg-staging-intel/payload/usr/local/bin/obsidian-installer
          chmod +x pkg-staging-intel/payload/usr/local/bin/obsidian-installer

          cp python-tools-intel/obsidian-server-macos-intel pkg-staging-intel/payload/usr/local/bin/obsidian-server
          chmod +x pkg-staging-intel/payload/usr/local/bin/obsidian-server

          cat > pkg-staging-intel/scripts/postinstall << 'EOF'
          #!/bin/bash
          ln -sf /usr/local/bin/obsidian-installer /usr/local/bin/obsidian-neural
          echo "OBSIDIAN-Neural (Intel) installed successfully!"
          exit 0
          EOF
          chmod +x pkg-staging-intel/scripts/postinstall

          pkgbuild \
            --root pkg-staging-intel/payload \
            --identifier com.obsidianneural.installer.intel \
            --version "1.0.${{ github.run_number }}" \
            --scripts pkg-staging-intel/scripts \
            --install-location / \
            OBSIDIAN-Neural-Installer-macOS-Intel.pkg

          # ARM PKG
          mkdir -p pkg-staging-arm64/payload/usr/local/bin
          mkdir -p pkg-staging-arm64/scripts

          cp python-tools-arm64/obsidian-installer-macos-arm64 pkg-staging-arm64/payload/usr/local/bin/obsidian-installer
          chmod +x pkg-staging-arm64/payload/usr/local/bin/obsidian-installer

          cp python-tools-arm64/obsidian-server-macos-arm64 pkg-staging-arm64/payload/usr/local/bin/obsidian-server
          chmod +x pkg-staging-arm64/payload/usr/local/bin/obsidian-server

          cat > pkg-staging-arm64/scripts/postinstall << 'EOF'
          #!/bin/bash
          ln -sf /usr/local/bin/obsidian-installer /usr/local/bin/obsidian-neural
          echo "OBSIDIAN-Neural (Apple Silicon) installed successfully!"
          exit 0
          EOF
          chmod +x pkg-staging-arm64/scripts/postinstall

          pkgbuild \
            --root pkg-staging-arm64/payload \
            --identifier com.obsidianneural.installer.arm64 \
            --version "1.0.${{ github.run_number }}" \
            --scripts pkg-staging-arm64/scripts \
            --install-location / \
            OBSIDIAN-Neural-Installer-macOS-ARM64.pkg

      - name: Upload PKGs
        uses: actions/upload-artifact@v4
        with:
          name: ObsidianNeural-macOS-PKGs
          path: |
            OBSIDIAN-Neural-Installer-macOS-Intel.pkg
            OBSIDIAN-Neural-Installer-macOS-ARM64.pkg

  commit-binaries:
    needs: [build-python-executables]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    concurrency:
      group: commit-binaries
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0

      - name: Download Python artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: ObsidianNeural-Python-*
          merge-multiple: false

      - name: Organize binaries
        run: |
          rm -rf bin/*
          mkdir -p bin

          if [ -d "ObsidianNeural-Python-macos-intel" ]; then
            cp ObsidianNeural-Python-macos-intel/obsidian-server-macos-intel bin/OBSIDIAN-Neural-Server-macos-intel
          fi

          if [ -d "ObsidianNeural-Python-macos-arm64" ]; then
            cp ObsidianNeural-Python-macos-arm64/obsidian-server-macos-arm64 bin/OBSIDIAN-Neural-Server-macos-arm64
          fi

          if [ -d "ObsidianNeural-Python-linux" ]; then
            cp ObsidianNeural-Python-linux/obsidian-server-linux bin/OBSIDIAN-Neural-Server-linux
          fi

      - name: Commit binaries
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          git add bin/
          git commit -m "Update binaries [skip ci]" || exit 0

          git pull --rebase origin main
          git push

  create-release:
    needs:
      [
        build-windows,
        build-macos-universal,
        test-macos-universal,
        build-linux,
        build-python-executables,
        build-macos-packages,
        commit-binaries,
      ]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create release assets
        run: |
          # Copy installer.py for Windows users
          cp installer.py ./OBSIDIAN-Neural-Installer.py

          # VST3 plugins - UN SEUL BUILD UNIVERSEL
          if [ -d "ObsidianNeural-Windows-VST3" ]; then
            cd ObsidianNeural-Windows-VST3
            zip -r ../OBSIDIAN-Neural-Windows-VST3.zip .
            cd ..
          fi

          # Build universel macOS (compatible 10.15+)
          if [ -d "ObsidianNeural-macOS-Universal-VST3" ]; then
            cd ObsidianNeural-macOS-Universal-VST3
            zip -r ../OBSIDIAN-Neural-macOS-VST3.zip .
            cd ..
          fi

          if [ -d "ObsidianNeural-Linux-VST3" ]; then
            cd ObsidianNeural-Linux-VST3
            tar -czf ../OBSIDIAN-Neural-Linux-VST3.tar.gz .
            cd ..
          fi

          # DMGs et PKGs
          if [ -d "ObsidianNeural-macOS-DMGs" ]; then
            cp ObsidianNeural-macOS-DMGs/*.dmg ./
          fi

          if [ -d "ObsidianNeural-macOS-PKGs" ]; then
            cp ObsidianNeural-macOS-PKGs/*.pkg ./
          fi

          # Linux installer
          if [ -d "ObsidianNeural-Python-linux" ]; then
            cp ObsidianNeural-Python-linux/obsidian-installer-linux ./OBSIDIAN-Neural-Installer-Linux
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: "OBSIDIAN-Neural v${{ github.run_number }}"
          body: |
            # 🎵 OBSIDIAN-Neural Release v${{ github.run_number }}

            ## 📥 Quick Installation Guide

            ### 🎯 **Recommended: Cloud API** (Easiest - No GPU Required)

            #### **Windows**
            1. Download `OBSIDIAN-Neural-Windows-VST3.zip`
            2. Extract to `C:\Program Files\Common Files\VST3\`
            3. Get your API key at [obsidian-neural.com](https://obsidian-neural.com)
            4. Load in your DAW and configure

            #### **macOS** (Works on 10.15 Catalina → 15.0 Sequoia)
            1. Download `OBSIDIAN-Neural-macOS-VST3.zip`
            2. Install VST3:
            ```bash
            sudo cp -r ~/Downloads/OBSIDIAN-Neural.vst3 /Library/Audio/Plug-Ins/VST3/
            sudo xattr -cr /Library/Audio/Plug-Ins/VST3/OBSIDIAN-Neural.vst3
            sudo chmod +x /Library/Audio/Plug-Ins/VST3/OBSIDIAN-Neural.vst3/Contents/MacOS/OBSIDIAN-Neural
            ```
            3. Get your API key at [obsidian-neural.com](https://obsidian-neural.com)
            4. Load in your DAW and configure

            **⚠️ macOS Security Note**: 
            - First time: Right-click plugin in DAW → Open
            - If blocked: System Settings → Privacy & Security → Allow
            - Code signing coming soon!

            #### **Linux**
            1. Download `OBSIDIAN-Neural-Linux-VST3.tar.gz`
            2. Install:
            ```bash
            mkdir -p ~/.vst3
            tar -xzf OBSIDIAN-Neural-Linux-VST3.tar.gz -C ~/.vst3/
            ```
            3. Get your API key at [obsidian-neural.com](https://obsidian-neural.com)
            4. Load in your DAW and configure

            ---

            ### 🔧 **Advanced: Self-Hosted Server**

            For unlimited generations and complete privacy:

            **Requirements:**
            - NVIDIA GPU with CUDA
            - Python 3.10+
            - ~10GB disk space

            **Quick Setup:**
            - **Windows**: Run `python OBSIDIAN-Neural-Installer.py`
            - **macOS Intel**: Install `OBSIDIAN-Neural-Installer-macOS-Intel.dmg`
            - **macOS ARM**: Install `OBSIDIAN-Neural-Installer-macOS-ARM64.dmg`
            - **Linux**: Run `./OBSIDIAN-Neural-Installer-Linux`

            ---

            ### 📊 Build Information

            **macOS Compatibility:**
            - ✅ macOS 10.15 Catalina
            - ✅ macOS 11 Big Sur
            - ✅ macOS 12 Monterey
            - ✅ macOS 13 Ventura
            - ✅ macOS 14 Sonoma
            - ✅ macOS 15 Sequoia

            **Architectures:**
            - Intel x86_64
            - Apple Silicon arm64

            **Build Details:**
            - Deployment Target: 10.15
            - JUCE Version: 8.0.8
            - Build Type: Release
            - Optimization: Full

            ---

            ### 🐛 Known Issues

            - macOS: Not code-signed yet (manual security approval needed)

            ---

            ### 📚 Documentation

            - 📘 [Getting Started Guide](https://github.com/innermost47/ai-dj#readme)
            - 🎓 [Video Tutorial](https://www.youtube.com/watch?v=l4KMC5adxVA)
            - 💬 [GitHub Discussions](https://github.com/innermost47/ai-dj/discussions)
            - 🐛 [Report Issues](https://github.com/innermost47/ai-dj/issues)

            ---

            ### 🙏 Support the Project

            ⭐ Star the repo if you find OBSIDIAN-Neural useful!

            Questions? Join the discussion or email: b03caa1n5@mozmail.com

          files: |
            OBSIDIAN-Neural-Installer.py
            OBSIDIAN-Neural-Windows-VST3.zip
            OBSIDIAN-Neural-macOS-VST3.zip
            OBSIDIAN-Neural-Linux-VST3.tar.gz
            OBSIDIAN-Neural-Installer-macOS-Intel.dmg
            OBSIDIAN-Neural-Installer-macOS-ARM64.dmg
            OBSIDIAN-Neural-Installer-macOS-Intel.pkg
            OBSIDIAN-Neural-Installer-macOS-ARM64.pkg
            OBSIDIAN-Neural-Installer-Linux
